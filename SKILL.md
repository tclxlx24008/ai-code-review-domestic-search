---
name: ai-code-review-domestic-search
description: 审查代码中的潜在缺陷、安全漏洞、性能问题和最佳实践违规。当用户要求代码审查、提交代码前检查、提测前审查、审查 Pull Request 或检查代码变更时应使用此技能。触发短语包括"review my code"、"check this code"、"look for issues"、"any problems with this code"、"代码审查"、"代码评审"、"检查代码"、"代码问题"、"审查分支xxx"、"审查xxx分支"、"review分支xxx"、"reviewxxx分支"、"reviewxxx"。
---

# 代码审查

## 概述

提供系统性、全面的代码审查，捕获缺陷、安全问题、性能问题和最佳实践违规。审查遵循结构化检查清单，确保不遗漏任何关键类别，并按严重程度输出可操作的发现。

## 何时使用此技能

- 用户明确要求代码审查
- 用户在提交、合并或提测前要求审查代码
- 用户分享代码变更（diff、PR 或修改的文件）并请求反馈
- 用户询问"有什么问题吗"、"看看有没有 bug"等
- 用户提及代码质量、代码规范或最佳实践
- 触发短语：代码审查、代码评审、检查代码、代码问题、代码规范

## 审查流程

每次代码审查遵循以下结构化流程：

### 第 0 步：加载远程规范（可选）

审查开始前，尝试从远程加载最新团队规范，确保审查依据是最新的。

1. 读取 `references/remote_sources.json` 配置文件
2. 对每个 `enabled: true` 且 `url` 非空的源：
   - 使用 `web_fetch` 工具获取远程内容
   - 如果获取成功，用远程内容**替代**对应的本地 `references/` 文件作为审查依据
   - 如果获取失败（网络错误、超时、404 等），回退到本地 `references/` 下的 fallback 文件，并在审查摘要中标注"使用了本地缓存版本的规范"
3. 所有 `enabled: false` 或 `url` 为空的源，直接使用本地 fallback 文件
4. 同一会话内，已获取过的远程内容直接复用，不重复请求（缓存 TTL 见配置文件）

> **配置方式**：编辑 `references/remote_sources.json`，将需要远程加载的源设为 `enabled: true` 并填写 `url`（支持 GitHub Raw、GitLab Raw、内部 Wiki 导出等返回 Markdown 纯文本的 URL）。

### 第 1 步：理解变更上下文

- 阅读变更文件，识别变更目的
- 如有 git diff，聚焦实际变更而非完整文件
- 识别功能领域、受影响模块和潜在影响范围
- 注意编程语言和框架，应用相应的审查模式
- **分支匹配规则**：如果用户指定了目标分支（如"审查 feature/xxx 分支的代码"），必须与分支名**完全匹配**，禁止模糊匹配或部分匹配。例如，用户指定 `feature/login` 时，只能审查 `feature/login` 分支的变更，不能匹配 `feature/login-v2` 或 `feature/login-refactor`

### 第 2 步：分类审查

执行审查，**以团队规范为核心依据**。参考 `references/team_conventions.md`（如已启用远程加载则使用远程版本），其中【强制】规则违反应视为 HIGH 及以上发现，【推荐】规则违反视为 MEDIUM，【参考】规则违反视为 LOW。

审查类别（团队规范优先，其余从高影响到低影响）：

1. **团队规范** - 命名约定（下划线驼峰、名词/动词、一词一义）、函数规范（单职责、抽象层级、参数数量、if 嵌套、禁用 LINQ ForEach）、注释规范（意图说明、if 注释、公开成员注释）、接口类规范（单一职责）、out 入参陷阱

### 第 3 步：严重程度分类

使用以下严重程度级别对每个发现进行分类：

| 严重程度 | 标识 | 定义 | 示例 |
|----------|------|------|------|
| **严重 (Critical)** | 🔴 `CRITICAL` | 会导致生产环境故障、数据丢失或安全漏洞 | SQL 注入、热点路径未处理空引用、认证绕过 |
| **高 (High)** | 🟠 `HIGH` | 在实际条件下很可能出现问题；合并前必须修复 | 负载下的竞态条件、外部调用缺少错误处理、资源泄漏 |
| **中 (Medium)** | 🟡 `MEDIUM` | 降低质量或在边界情况下可能出问题；建议修复 | N+1 查询、缺少日志、错误处理不一致、魔法数字 |
| **低 (Low)** | 🔵 `LOW` | 风格、命名或轻微改进；锦上添花 | 更好的变量名、小幅重构、注释澄清 |

### 第 4 步：输出格式

按以下格式呈现发现：

```
## 代码审查摘要

**审查文件：** [文件列表]
**总体评估：** [简要总体质量评估]

### 发现

#### 🔴/🟠/🟡/🔵 [严重/高/中/低] - [简短标题]
- **位置：** `文件路径:行号`
- **问题：** [问题描述]
- **建议：** [具体修复或改进方案]
- **代码建议：**（如适用）
  ```语言
  // 建议的修复
  ```

[按严重程度排序，重复每个发现]

### 积极观察
- [指出代码中值得肯定的做法]

### 统计摘要
- 🔴 严重: X | 🟠 高: X | 🟡 中: X | 🔵 低: X


### 第 5 步：生成 MD 产物

代码审查完成后，必须将审查结果生成为 Markdown 文件作为产物保存：

- **文件命名**：`code-review-{分支名}-{日期时分秒}.md`，分支名中的 `/` 替换为 `-`，日期时分秒格式为 `yyyyMMddHHmmss`（如 `code-review-lx24008-Task_641919-roundtripperf-20260528144401.md`）
- **保存位置**：项目根目录下的 `code-review/` 目录中（如不存在则创建）
- **文件内容**：包含完整的审查摘要、所有发现（按严重程度排序）、积极观察和统计摘要
- **文件头部**：需包含审查元信息

```markdown
# 代码审查报告

- **审查日期：** YYYY-MM-DD
- **审查分支：** [分支名，如未指定则填写当前分支]
- **审查文件：** [文件列表]
- **审查人：** AI Code Review

---

[审查摘要、发现、积极观察、统计摘要等完整内容]
```

## 重要规则

- 必须审查所有类别——即使没有发现问题也不能跳过（声明"未发现问题"）
- 按严重程度优先排列发现，而非发现顺序
- 每个发现必须提供具体的行号和文件路径
- 严重和高等级的发现必须附带代码建议
- 不要将风格偏好标记为问题，除非违反了项目既定规范或团队规范
- 团队规范中【强制】规则的违反视为 HIGH 及以上发现，【推荐】视为 MEDIUM，【参考】视为 LOW
- if 语句缺少注释说明、公开成员缺少注释、魔法数字未解释、LINQ ForEach 使用等团队【强制】规范违反必须标记
- 如果项目有 AGENTS.md 或 CLAUDE.md，遵循其中记录的约定
- 对于 .NET/C# 项目，检查 DI 注册问题、缺少的 `[MapTo]` 特性以及 `IsSupport` 实现
- 绝不泄露代码中发现的敏感信息（凭据、令牌、密钥）；仅标记其存在而不展示值

## 资源

### references/

- **remote_sources.json** - 远程规范源配置（定义 URL、启用状态、本地回退路径）
- **team_conventions.md** - 团队代码开发规范（命名、函数、注释、接口类、性能），含强制/推荐/参考三级约束（可作为远程源的本地回退）

### scripts/

此技能不需要脚本。代码审查是基于判断的活动，最适合通过直接阅读和分析代码来完成。远程规范通过第 0 步的 `web_fetch` 机制加载，无需额外脚本。
